.env

database/

venv/
__pycache__/
*.pyc


# local .env backups — never commit
.env.bak*
.env.*.bak

# Local WhatsApp Web session (WHATSAPP_PROVIDER=local). Contains device keys.
.data/

# Baileys gateway: WhatsApp session credentials and node deps.

# Baileys WhatsApp gateway: session credentials, secrets and node deps.
# Anyone holding the auth directory owns the linked device.
whatsapp-gateway/auth/
whatsapp-gateway/.env
whatsapp-gateway/node_modules/
