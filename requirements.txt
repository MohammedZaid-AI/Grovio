# Direct dependencies only - let pip resolve the transitive tree.

# Web
fastapi==0.138.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32
python-dotenv==1.2.2

# LLM (Groq is reached via its OpenAI-compatible endpoint - one client, one path)
openai==2.9.0

# Provider integrations (MCP)
mcp==1.27.2
mcp-use==1.7.0
httpx==0.28.1

# Voice notes: speech to text and text to speech, Kannada + English.
# OPTIONAL at runtime - with no SARVAM_API_KEY the concierge answers a voice
# note in text rather than failing. The distribution is `sarvamai`.
sarvamai==0.1.32

# Credential encryption at rest
cryptography==48.0.0

# WhatsApp Business Cloud API is called over plain HTTP via httpx — there is
# no vendor SDK to install.
#
# WHATSAPP_PROVIDER=local (development only) additionally needs:
#   pip install neonize qrcode
# Deliberately NOT pinned here — production must not carry an
# unofficial WhatsApp client. See CLAUDE.md.
